name: delivery-management

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: delivery
      POSTGRES_PASSWORD: delivery
      POSTGRES_DB: delivery
    ports:
      # Host-side 5433 to avoid clashing with a native Postgres install
      # already using 5432 on the host. The container's internal port
      # (and the `backend` service's DATABASE_URL, which talks to it over
      # the docker network as `db:5432`) is unaffected.
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U delivery"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://delivery:delivery@db:5432/delivery?schema=public
      JWT_SECRET: change-me-in-production
      PORT: 4000
      CORS_ORIGIN: http://localhost:5173
      AVG_SPEED_KMH: 28
      STOP_HANDLING_MINUTES: 5
    ports:
      - "4000:4000"

volumes:
  db_data:
